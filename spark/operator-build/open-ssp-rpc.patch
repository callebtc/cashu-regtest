diff --git a/protos/spark.proto b/protos/spark.proto
--- a/protos/spark.proto
+++ b/protos/spark.proto
@@ -107,0 +108,5 @@ service SparkService {
+    // Initiates the SSP counter transfer that atomically settles both sides of
+    // a Swap V3 operation. The authenticated sender must be the receiver of
+    // the referenced primary transfer.
+    rpc initiate_swap_counter_transfer(InitiateSwapCounterTransferRequest) returns (StartTransferResponse) {}
+
@@ -2188,0 +2194,6 @@ message InstallLeafDecompositionsResponse {
+
+message InitiateSwapCounterTransferRequest {
+    StartTransferRequest transfer = 1;
+    AdaptorPublicKeyPackage adaptor_public_keys = 2;
+    string primary_transfer_id = 3;
+}
diff --git a/spark/so/grpc/spark_server.go b/spark/so/grpc/spark_server.go
--- a/spark/so/grpc/spark_server.go
+++ b/spark/so/grpc/spark_server.go
@@ -11,0 +12 @@ import (
+	pbinternal "github.com/lightsparkdev/spark/proto/spark_internal"
@@ -355,0 +357,10 @@ func (s *SparkServer) InitiateSwapPrimaryTransfer(ctx context.Context, req *pb.I
+// InitiateSwapCounterTransfer exposes the open-source consensus counter leg
+// using the public Spark protocol shape consumed by open SSP implementations.
+func (s *SparkServer) InitiateSwapCounterTransfer(ctx context.Context, req *pb.InitiateSwapCounterTransferRequest) (*pb.StartTransferResponse, error) {
+	return handler.InitiateCounterTransferConsensus(ctx, s.config, &pbinternal.InitiateCounterTransferRequest{
+		Transfer:          req.GetTransfer(),
+		AdaptorPublicKeys: req.GetAdaptorPublicKeys(),
+		PrimaryTransferId: req.GetPrimaryTransferId(),
+	})
+}
+
diff --git a/spark/so/handler/swap_counter_transfer_flow_handler.go b/spark/so/handler/swap_counter_transfer_flow_handler.go
--- a/spark/so/handler/swap_counter_transfer_flow_handler.go
+++ b/spark/so/handler/swap_counter_transfer_flow_handler.go
@@ -455 +455 @@ func (f *swapCounterTransferCoordinatorFlow) RollbackPayload() proto.Message {
-// initiateCounterTransferConsensus runs the swap v3 counter leg through the
+// InitiateCounterTransferConsensus runs the swap v3 counter leg through the
@@ -463 +463 @@ func (f *swapCounterTransferCoordinatorFlow) RollbackPayload() proto.Message {
-func initiateCounterTransferConsensus(ctx context.Context, config *so.Config, req *pbinternal.InitiateCounterTransferRequest) (*pb.StartTransferResponse, error) {
+func InitiateCounterTransferConsensus(ctx context.Context, config *so.Config, req *pbinternal.InitiateCounterTransferRequest) (*pb.StartTransferResponse, error) {
diff --git a/spark/so/rpcpolicy/policy.go b/spark/so/rpcpolicy/policy.go
--- a/spark/so/rpcpolicy/policy.go
+++ b/spark/so/rpcpolicy/policy.go
@@ -177,0 +178 @@ func sparkServicePolicies() map[string]Policy {
+		pbspark.SparkService_InitiateSwapCounterTransfer_FullMethodName:         {AuthMode: AuthSession},
